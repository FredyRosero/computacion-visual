## Punto 1- Python

### Dependencias

Para ejecutar el notebook de Python, asegúrate de tener instaladas las siguientes librerías:

```bash
sudo apt-get update && sudo apt-get install -y libgl1 libglib2.0-0
pip install opencv-python matplotlib numpy imageio
```